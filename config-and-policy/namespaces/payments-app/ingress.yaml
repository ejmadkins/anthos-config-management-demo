apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: payments-app-ingress
spec:
  backend:
    serviceName: payments-app-service
    servicePort: 60001
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: payments-app-service
          servicePort: 60001